import java.util.Stack;

public class ValidParenthesisProblem {

    /**
     * Given a string with opening and closing brackets, we have to identify if any brackets is missing
     * is it a valid parenthesis.
     */

    public static void main(String[] args) {
        String data = "{}[{()()}]";
    }

    public boolean validParenthesisString(String input) {
        Stack<Character> charStack = new Stack<>();

        for(int i = 0; i < input.length(); i++) {

        }
        return true;
    }
}
